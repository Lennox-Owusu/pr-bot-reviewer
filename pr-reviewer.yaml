# Phase 2 / E1 — config enforcement test. Push this to MAIN (base branch).
# Adds an ignore pattern: findings on files matching "ignored_*.py" must be dropped.
enabled: true
dry_run: false
comment_severity_threshold: warning
review_settings:
  enabled: true
  max_comments: 10
  comment_severity_threshold: warning
file_rules:
  enabled: true
  ignored_files:
    - "ignored_*.py"
