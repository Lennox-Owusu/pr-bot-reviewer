# PROD-4 live repro — INVALID config (review_settings missing the required
# max_files_per_review field). Push to MAIN to reproduce; restore a valid one after.
enabled: true
dry_run: false
comment_severity_threshold: warning
review_settings:
  enabled: true
  max_comments: 10
  comment_severity_threshold: warning
file_rules:
  enabled: true
